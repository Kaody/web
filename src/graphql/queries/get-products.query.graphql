query getProducts($input: GetProductsInput!) {
  getProducts(input: $input) {
    _id
    name
    price
    quantity
    status
    images
    tags
    attributes {
      name
      value
    }
    category {
      name
    }
    store {
      _id
      name
    }
  }
}
