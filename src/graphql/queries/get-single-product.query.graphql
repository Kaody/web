query getSingleProduct($input: GetProductInput!) {
  getProduct(input: $input) {
    name
    description
    price
    quantity
    status
    images
    category {
      name
    }
    attributes {
      name
      value
    }
    store {
      name
      _id
    }
  }
}
